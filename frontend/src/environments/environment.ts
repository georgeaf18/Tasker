/**
 * Production Environment Configuration
 *
 * PLACEHOLDER VALUES - Update with real credentials from SECURITY_REFERENCE.md
 */
export const environment = {
  production: true,
  apiUrl: 'https://PRODUCTION_BACKEND_URL/api', // e.g., https://tasker-backend.fly.dev/api
  apiKey: 'PLACEHOLDER_PRODUCTION_API_KEY', // Must match backend production API_KEY
};
