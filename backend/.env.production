# Production Environment (Neon Production Branch)
# NEVER commit real credentials! Set this via environment variables in your deployment platform
NODE_ENV=production
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
